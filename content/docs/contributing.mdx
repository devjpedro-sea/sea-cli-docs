---
title: Guia de Contribuição
description: Como contribuir para o desenvolvimento do SEA CLI
---

# Guia de Contribuição

Obrigado por considerar contribuir com o SEA CLI! Este guia ajudará você a começar.

## 🌊 Código de Conduta

Ao contribuir para este projeto, você concorda em seguir nosso [Código de Conduta](https://www.contributor-covenant.org/pt-br/version/2/1/code_of_conduct/).

### Nossos Valores

- **Respeito**: Trate todos com respeito e empatia
- **Colaboração**: Trabalhe em conjunto para melhorar o projeto
- **Qualidade**: Mantenha altos padrões de código e documentação
- **Transparência**: Comunique-se abertamente sobre mudanças

## 🚀 Como Contribuir

### Reportar Bugs

Encontrou um bug? Ajude-nos a corrigi-lo!

1. **Verifique** se já foi reportado nas [Issues](https://gitlab.seatecnologia.com.br/sea-cli/issues)
2. **Crie** uma nova issue com o template de bug
3. **Inclua**:
   - Descrição clara do problema
   - Passos para reproduzir
   - Comportamento esperado vs atual
   - Versões (SEA CLI, Node.js, SO)
   - Logs com `--verbose`

#### Template de Bug Report

````markdown
## Descrição

[Descrição clara do bug]

## Passos para Reproduzir

1. Execute `sea init`
2. Configure com X
3. Erro aparece

## Comportamento Esperado

[O que deveria acontecer]

## Comportamento Atual

[O que está acontecendo]

## Ambiente

- SEA CLI: 2.0.0
- Node.js: 18.17.0
- npm: 9.6.7
- SO: Ubuntu 22.04

## Logs

```bash
$ sea init --verbose
[cole os logs aqui]
```
````

## Screenshots

[Se aplicável]

````

### Sugerir Features

Tem uma ideia para melhorar o SEA CLI?

1. **Verifique** se já foi sugerida nas [Discussions](https://gitlab.seatecnologia.com.br/sea-cli/discussions)
2. **Crie** uma discussion explicando:
   - Qual problema a feature resolve
   - Como deveria funcionar
   - Exemplos de uso
   - Alternativas consideradas

### Contribuir com Código

#### 1. Fork e Clone

```bash
# Fork no GitLab
# Depois clone seu fork

git clone https://gitlab.seatecnologia.com.br/seu-usuario/sea-cli.git
cd sea-cli

# Adicione o remote upstream
git remote add upstream https://gitlab.seatecnologia.com.br/sea-tecnologia/sea-cli.git
````

#### 2. Configurar Ambiente

```bash
# Instalar dependências
npm install

# Rodar testes
npm test

# Build
npm run build

# Link local para testar
npm link
```

#### 3. Criar Branch

```bash
# Atualizar main
git checkout main
git pull upstream main

# Criar branch de feature
git checkout -b feature/minha-feature

# Ou branch de bugfix
git checkout -b fix/meu-bug
```

#### 4. Fazer Mudanças

**Siga as boas práticas:**

- ✅ Escreva código limpo e legível
- ✅ Adicione testes para novas features
- ✅ Atualize documentação
- ✅ Siga o style guide do projeto
- ✅ Mantenha commits atômicos

#### 5. Testar

```bash
# Rodar todos os testes
npm test

# Testes específicos
npm test -- nome-do-arquivo

# Coverage
npm run test:coverage

# Lint
npm run lint

# Type check
npm run type-check
```

#### 6. Commit

Usamos [Conventional Commits](https://www.conventionalcommits.org/pt-br/):

```bash
# Formato
<tipo>(<escopo>): <descrição>

# Exemplos
feat(cli): adiciona comando create-turbo-workspace
fix(template): corrige geração de eslintrc
docs(readme): atualiza instruções de instalação
test(init): adiciona testes para comando init
refactor(core): melhora performance do template engine
chore(deps): atualiza dependências
```

**Tipos válidos:**

- `feat`: Nova funcionalidade
- `fix`: Correção de bug
- `docs`: Documentação
- `style`: Formatação (sem mudança de código)
- `refactor`: Refatoração
- `test`: Adicionar/modificar testes
- `chore`: Manutenção (deps, config, etc)
- `perf`: Melhoria de performance
- `ci`: Mudanças em CI/CD

#### 7. Push e Pull Request

```bash
# Push para seu fork
git push origin feature/minha-feature

# Criar Pull Request no GitLab
# Preencha o template de PR
```

#### Template de Pull Request

```markdown
## Descrição

[Descreva as mudanças]

## Tipo de Mudança

- [ ] 🐛 Bug fix
- [ ] ✨ Nova feature
- [ ] 🔧 Breaking change
- [ ] 📚 Documentação

## Checklist

- [ ] Testes adicionados/atualizados
- [ ] Documentação atualizada
- [ ] Lint passou (`npm run lint`)
- [ ] Testes passaram (`npm test`)
- [ ] Build passou (`npm run build`)
- [ ] Commit seguem conventional commits

## Issues Relacionadas

Closes #123
Related to #456

## Screenshots

[Se aplicável]

## Notas para Revisores

[Informações adicionais]
```

## 📁 Estrutura do Projeto

```
sea-cli/
  src/
    commands/          # Comandos CLI
      init.ts
      create-workspace.ts
      create-module.ts
    templates/         # Templates de arquivos
      eslint/
      prettier/
      jest/
    utils/             # Utilitários
      file-manager.ts
      logger.ts
      validator.ts
    core/              # Core da CLI
      cli.ts
      template-engine.ts
    types/             # TypeScript types
      index.ts
  tests/               # Testes
    unit/
    integration/
    e2e/
  docs/                # Documentação
  examples/            # Exemplos
  scripts/             # Scripts de build/deploy
  .husky/              # Git hooks
  package.json
  tsconfig.json
  jest.config.js
  README.md
```

## 🧪 Testes

### Estrutura de Testes

```typescript
// tests/unit/commands/init.test.ts
import { InitCommand } from "../../../src/commands/init";

describe("InitCommand", () => {
  describe("run()", () => {
    it("deve criar arquivos de configuração", async () => {
      const cmd = new InitCommand();
      await cmd.run({ eslint: true, prettier: true });

      expect(fs.existsSync(".eslintrc.js")).toBe(true);
      expect(fs.existsSync(".prettierrc")).toBe(true);
    });

    it("deve lançar erro se Node.js < 16", async () => {
      const cmd = new InitCommand();

      await expect(cmd.run()).rejects.toThrow("Node.js 16+ é necessário");
    });
  });
});
```

### Rodar Testes

```bash
# Todos os testes
npm test

# Watch mode
npm test -- --watch

# Coverage
npm run test:coverage

# Apenas unit
npm test -- --testPathPattern=unit

# Apenas integration
npm test -- --testPathPattern=integration

# Com verbose
npm test -- --verbose
```

### Coverage Mínimo

- **Branches**: 80%
- **Functions**: 80%
- **Lines**: 80%
- **Statements**: 80%

## 📝 Documentação

### Atualizar Docs

Documentação está em `/docs` e usa Fumadocs:

```bash
cd docs
npm install
npm run dev

# Build
npm run build
```

### Adicionar Nova Página

1. Criar arquivo MDX em `content/docs/`
2. Adicionar ao `meta.json`
3. Testar localmente
4. Submeter PR com a mudança

### Exemplo de Documentação

````mdx
---
title: Título da Página
description: Descrição para SEO
---

# Título Principal

Introdução breve.

## Seção

Conteúdo...

### Subseção

Mais conteúdo...

## Exemplos

```bash
# Exemplo de comando
sea init --eslint
```
````

## Boas Práticas

<Callout type="tip">Dica útil aqui</Callout>

<Callout type="warning">Aviso importante</Callout>

## Recursos Relacionados

- [Outro Guide](/docs/guides/outro)
- [API Reference](/docs/api-reference)

````

## 🎨 Style Guide

### TypeScript

```typescript
// ✅ Bom
export class SeaCLI {
  private readonly config: SeaConfig;

  constructor(options?: SeaCLIOptions) {
    this.config = this.loadConfig(options);
  }

  public async init(options: InitOptions): Promise<void> {
    Logger.info('Initializing...');

    try {
      await this.setupTools(options);
      Logger.success('✅ Initialization complete!');
    } catch (error) {
      Logger.error('Failed to initialize:', error);
      throw error;
    }
  }

  private loadConfig(options?: SeaCLIOptions): SeaConfig {
    return {
      cwd: options?.cwd ?? process.cwd(),
      verbose: options?.verbose ?? false,
    };
  }
}

// ❌ Evitar
export class SeaCLI {
  config: any;  // Usar tipos específicos

  constructor(options) {  // Sem tipos
    this.config = options;
  }

  init(options) {  // Sem async, sem tipos
    console.log('init');  // Usar Logger
    // sem tratamento de erro
  }
}
````

### Nomenclatura

```typescript
// Classes: PascalCase
class TemplateEngine {}

// Interfaces/Types: PascalCase com 'I' opcional
interface SeaConfig {}
type InitOptions = {};

// Funções/Métodos: camelCase
function createWorkspace() {}
async function setupTools() {}

// Variáveis: camelCase
const workspaceName = "my-workspace";
let isInitialized = false;

// Constantes: UPPER_SNAKE_CASE
const DEFAULT_PORT = 3000;
const MAX_RETRIES = 3;

// Arquivos: kebab-case
// file-manager.ts
// template-engine.ts
```

### Imports

```typescript
// ✅ Ordem correta
// 1. Node built-ins
import fs from "fs";
import path from "path";

// 2. External packages
import chalk from "chalk";
import inquirer from "inquirer";

// 3. Internal modules
import { Logger } from "./utils/logger";
import { FileManager } from "./utils/file-manager";

// 4. Types
import type { SeaConfig } from "./types";
```

### Error Handling

```typescript
// ✅ Bom
try {
  await riskyOperation();
} catch (error) {
  if (error instanceof SeaError) {
    Logger.error(error.message);
    throw error;
  }

  Logger.error("Unexpected error:", error);
  throw new SeaError("Operation failed", { cause: error });
}

// ❌ Evitar
try {
  await riskyOperation();
} catch (e) {
  console.log(e); // Usar Logger
  // Não re-throw
}
```

## 🔄 Processo de Review

### O que Avaliamos

1. **Funcionalidade**

   - A feature funciona conforme esperado?
   - Resolve o problema proposto?
   - Não quebra funcionalidades existentes?

2. **Código**

   - Segue o style guide?
   - É legível e manutenível?
   - Está bem documentado?
   - Tem testes adequados?

3. **Performance**

   - Não degrada performance?
   - Usa recursos eficientemente?
   - Escala bem?

4. **Segurança**
   - Não introduz vulnerabilidades?
   - Valida inputs adequadamente?
   - Trata erros corretamente?

### Timeline

- **Review inicial**: 2-3 dias úteis
- **Feedback**: Respondemos dentro de 1 dia
- **Merge**: Após aprovação e CI passar

### Aprovação

PRs precisam de:

- ✅ 2 aprovações de mantenedores
- ✅ CI pipeline verde
- ✅ Sem conflitos
- ✅ Changelog atualizado (se feature/fix)

## 🏆 Reconhecimento

Contribuidores são creditados em:

- `CONTRIBUTORS.md`
- Release notes
- Documentação

Top contributors ganham:

- Badge de contributor
- Acesso prioritário a betas
- Menção em redes sociais

## 📞 Contato

### Canais de Comunicação

- **Issues**: Para bugs e features
- **Discussions**: Para perguntas e ideias
- **Discord**: Para chat em tempo real
- **Email**: dev@seatecnologia.com.br

### Mantenedores

- **João Pedro** - [@joao-pedro](https://gitlab.seatecnologia.com.br/joao-pedro)
- **SEA Team** - [@sea-team](https://gitlab.seatecnologia.com.br/sea-team)

## 📚 Recursos

### Para Contribuidores

- [Conventional Commits](https://www.conventionalcommits.org/pt-br/)
- [Semantic Versioning](https://semver.org/lang/pt-BR/)
- [Keep a Changelog](https://keepachangelog.com/pt-BR/)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)
- [Jest Documentation](https://jestjs.io/pt-BR/)

### Projetos Relacionados

- [Liferay DXP](https://learn.liferay.com/)
- [Turborepo](https://turbo.build/repo/docs)
- [ESLint](https://eslint.org/)
- [Prettier](https://prettier.io/)

## 🎉 Primeiros Passos

### Issues para Iniciantes

Procure por labels:

- `good first issue` - Ideal para primeiro PR
- `help wanted` - Precisamos de ajuda
- `documentation` - Melhorias em docs

### Checklist para Primeira Contribuição

- [ ] Ler este guia completamente
- [ ] Configurar ambiente de desenvolvimento
- [ ] Escolher uma issue `good first issue`
- [ ] Comentar na issue sua intenção
- [ ] Fork e clonar o repositório
- [ ] Criar branch
- [ ] Fazer as mudanças
- [ ] Adicionar testes
- [ ] Rodar testes e lint
- [ ] Commit seguindo conventional commits
- [ ] Push e criar PR
- [ ] Aguardar review
- [ ] Responder feedback
- [ ] Celebrar! 🎉

## 🙏 Agradecimentos

Obrigado por contribuir para tornar o SEA CLI melhor para todos!

Toda contribuição, grande ou pequena, faz diferença. Seja ela:

- 🐛 Reportar um bug
- 💡 Sugerir uma feature
- 📝 Melhorar documentação
- 💻 Contribuir com código
- 🧪 Escrever testes
- 🎨 Melhorar UI/UX
- 🌍 Traduzir docs

**Você é incrível!** 🌊

---

**Dúvidas?** Abra uma [Discussion](https://gitlab.seatecnologia.com.br/sea-cli/discussions) ou entre em contato!
